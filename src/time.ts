/**
 * A day, in milliseconds: the one unit of time every package and app counts
 * windows and ages in (a Pass search's reach, the Ephemeris's freshness,
 * the windows Close approaches and Launches are held over), stated once
 * here, at the bottom of the packages, rather than in each that counts.
 */
export const DAY_MS = 86_400_000;

/** How long the Earth takes to turn once against the stars, in seconds: a Geostationary orbit's period. */
export const SIDEREAL_DAY_SECONDS = 86_164.0905;
